{
  "name": "liveavatar-sandbox-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "cd frontend && npm install && npm run build",
    "setup:context": "node scripts/setup-context.js"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "prisma": "^7.8.0"
  },
  "dependencies": {
    "@libsql/client": "^0.17.3",
    "@prisma/adapter-libsql": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "nodemailer": "^8.0.7"
  }
}
